'use client';

import React from 'react';

const AudioVisualizer: React.FC = () => {
    return (
        <div className="w-full h-16 bg-muted rounded mt-4 flex items-center justify-center">
            <span className="text-sm text-foreground">Audio Visualizer (Coming Soon)</span>
        </div>
    );
};

export default AudioVisualizer;
